server {
  server_name mfeller.io;
  listen 8080;
  root /var/www/blog;

  location / {
  }
}

server {
  server_name git.mfeller.io;
  listen 8080;
  root /var/www/git;

  location / {
  }
}

error_log /dev/stdout info;
access_log /dev/stdout;
